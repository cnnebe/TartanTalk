<!-- Adapted from: https://blog.heroku.com/real_time_rails_implementing_websockets_in_rails_5_with_action_cable -->
<ul class="nav navbar-nav">
  <% if current_user %>
   <li> 
    <%= link_to "New Chatroom", new_chatroom_path%>
   </li>
  <% end %>
  
  <% if logged_in? %>
  <li>
    <%= link_to "Chatrooms", chatrooms_path %>
  </li> 

  <li>
    <%= link_to "Profile", profile_path(current_user.id) %>
  </li> 
  <% end %>
  <li>
    <% if logged_in? && current_user.role != "student" %>
    <%= link_to "View all Users", users_path %>
    <% end %>
  </li> 

  <li>
    <% if logged_in? && current_user.role != "admin" %>
    <%= link_to "View all Counselors", staffs_path %>
    <% end %>
  </li> 

  <% if logged_in? && current_user.role == "admin" %>
    <li> <%= link_to "View all Staff", staffs_path %></li>
    <li> <%= link_to "Add New Counselor", new_staff_path %></li>
  <% end %>
</ul>

<ul class="nav navbar-nav navbar-right">
  <% if current_user %>
    <li>
      <%= link_to "Log Out", logout_path, method: "delete" %>
    </li>
  <% else %>
    <li><%= link_to "Sign Up", signup_path %></li>
    <li><%= link_to "Log In", login_path %></li>
  <% end %>
</ul>