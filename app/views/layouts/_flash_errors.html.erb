<% flash.each do |name, msg| %>
  <div class="alert alert-danger" data-options="animation_speed:500, animation:'fadeOut';" class="alert-box radius" <%= flash[:notice] ? "success" : "warning" %>>
    <a href="#" class="close">&times;</a>
    <%= msg %>
  </div>
<% end %>

